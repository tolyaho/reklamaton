# Required for chat, campaign drafts, profile extraction, and (when enabled) avatar images.
OPENAI_API_KEY=

# Optional. Comma-separated browser origins allowed by CORS.
# If unset, localhost:5174 and 127.0.0.1:5174 are allowed for local dev.
# CORS_ORIGINS=https://app.example.com,https://www.example.com
